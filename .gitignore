# Python on-site (ไม่ต้องขึ้น cloud)
main.py
config.json
static/
templates/
.venv/
__pycache__/
*.pyc
*.pyo

# Local env files (ห้ามขึ้น)
.env
.env.local
.env.*.local
api-gateway/.env
frontend/.env
frontend/.env.local

# CCTV person-detection model binaries (auto-download ตอนรัน — ไม่ commit, เก็บแค่ .pbtxt)
cctv/models/*.pb
cctv/models/*.tar.gz
cctv/models/_*.tmp

# PyInstaller build outputs (สร้างใหม่ได้จาก build_exe.bat)
cctv/build/
cctv/dist/
cctv/*.spec
cctv/Output/

# ไฟล์ติดตั้งที่ build แล้ว (binaries ใหญ่ — เก็บแค่ README)
cctv/release/*.exe
cctv/release/*.zip

# Build outputs & dependencies
node_modules/
frontend/dist/
frontend/node_modules/
api-gateway/node_modules/

# OS / Editor
.DS_Store
Thumbs.db
.vscode/
*.log
